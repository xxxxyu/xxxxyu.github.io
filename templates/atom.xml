<?xml version="1.0" encoding="UTF-8"?>
{% set home_url = config.base_url ~ "/" %}
{% if lang != config.default_language %}
  {% set_global home_url = config.base_url ~ "/" ~ lang ~ "/" %}
{% endif %}
<feed xmlns="http://www.w3.org/2005/Atom" xml:lang="{{ lang }}">
  <title>{{ config.title }}</title>
  <subtitle>{{ config.description }}</subtitle>
  <link rel="self" type="application/atom+xml" href="{{ feed_url | safe }}"/>
  <link rel="alternate" type="text/html" href="{{ home_url | safe }}"/>
  <generator uri="https://www.getzola.org/">Zola</generator>
  <updated>{{ last_updated | date(format="%+") }}</updated>
  <id>{{ feed_url | safe }}</id>
  {% for page in pages %}
  {% set page_updated = page.updated | default(value=page.date) %}
  <entry xml:lang="{{ page.lang }}">
    <title>{{ page.title }}</title>
    <published>{{ page.date | date(format="%+") }}</published>
    <updated>{{ page_updated | date(format="%+") }}</updated>
    <author><name>{% if page.authors %}{{ page.authors | first }}{% else %}{{ config.author }}{% endif %}</name></author>
    <link rel="alternate" type="text/html" href="{{ page.permalink | safe }}"/>
    <id>{{ page.permalink | safe }}</id>
    {% if page.description %}<summary>{{ page.description }}</summary>{% endif %}
    <content type="html" xml:base="{{ page.permalink | safe }}">{{ page.content }}</content>
  </entry>
  {% endfor %}
</feed>
